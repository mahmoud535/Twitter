<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.twitter">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/tweets"
        android:label="@string/app_name"
        android:roundIcon="@drawable/tweets"
        android:supportsRtl="true"
        android:theme="@style/Theme.Twitter">
        <activity android:name=".firestore.activities.activities.SettingsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Twitter.NoActionBar"/>
        <activity android:name=".firestore.activities.activities.UserProfileActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Twitter.NoActionBar"/>
        <activity android:name=".firestore.activities.activities.RegisterActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Twitter.NoActionBar"/>
        <activity android:name=".firestore.activities.activities.ForgotPasswordActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Twitter.NoActionBar"/>
        <activity android:name=".firestore.activities.activities.BaseActivity" />
        <activity android:name=".firestore.activities.activities.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Twitter.NoActionBar"/>
        <activity
            android:name=".firestore.activities.activities.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Twitter.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".firestore.activities.activities.MainActivity"
            android:screenOrientation="portrait"
            />
    </application>

</manifest>